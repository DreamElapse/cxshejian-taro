import request from '@/utils/api'

export default {
  getCity(data) {
      const url = ``
      return request.get({ url, data})
  },
  changeUserInfo(data) {
    const url = ``
    return request.get({ url, data})
  },
  verifyAuth(data) {
    const url = ``
    return request.get({ url, data})
  }

}
